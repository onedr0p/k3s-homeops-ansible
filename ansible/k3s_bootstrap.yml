---

- hosts: master
  gather_facts: no
  become: yes
  roles:
    - { role: k3s/config }

- hosts: localhost
  gather_facts: yes
  become: no
  roles:
    - { role: k3s/bootstrap }
    - { role: k3s/flux }
    - { role: k3s/helm, when: helm_operator == 'enabled' }
