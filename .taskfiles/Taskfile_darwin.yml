---
version: '3'

tasks:

  install:
    desc: Install required Homebrew apps
    cmds:
    - brew install ansible kubernetes-cli helm sshpass coreutils findutils

  install:dev:
    desc: Install required Homebrew apps for development
    cmds:
    - brew install pre-commit yamllint jq yq shellcheck
    - brew cask install virtualbox vagrant
