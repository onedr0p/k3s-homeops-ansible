---
apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    ipPools:
    - blockSize: 26
      cidr: "{{ k3s_service_cidr }}"
      encapsulation: IPIPCrossSubnet
      natOutgoing: Enabled
      nodeSelector: all()
