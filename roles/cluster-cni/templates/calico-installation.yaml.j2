---
apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    mtu: {{ calico.mtu }}
    ipPools:
    - blockSize: 26
      cidr: "{{ k3s_service_cidr }}"
      nodeSelector: all()
