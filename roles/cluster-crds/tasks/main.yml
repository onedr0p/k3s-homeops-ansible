---

- name: deploy crds
  become: false
  run_once: true
  delegate_to: localhost
  environment:
    KUBECONFIG: "../kubeconfig"
  command: "kubectl create -f {{ item }}"
  with_items:
  - "{{ crds }}"