master:
  hosts:
  vars:
    type: master

nodes:
  hosts:
  vars:
    type: worker

storage:
  hosts:
  vars:
    storage: ceph

k3s_cluster:
  children:
    other_group:
      children:
        master:
        nodes:

vault:
  hosts:
    localhost:
      ansible_connection: local
